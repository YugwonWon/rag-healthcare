# 치매노인 맞춤형 헬스케어 RAG 챗봇 - 메인 의존성

# ============================================
# Web Framework
# ============================================
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ============================================
# LLM & AI
# ============================================
httpx>=0.27.0
openai>=1.10.0
transformers>=4.36.0  # 한국어 NER 모델 (KoELECTRA)

# ============================================
# Embeddings (온디바이스)
# ============================================
sentence-transformers>=2.3.0
torch>=2.1.0

# ============================================
# LangChain + LangGraph
# ============================================
langchain>=0.1.0
langchain-community>=0.0.20
langchain-postgres>=0.0.6
langchain-text-splitters>=0.0.1
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0

# ============================================
# Knowledge Graph (GraphRAG + Neo4j)
# ============================================
neo4j>=5.0.0
langchain-neo4j>=0.1.0
langchain-experimental>=0.3.0
langchain-ollama>=0.2.0
psycopg[binary]>=3.1.0
psycopg2-binary>=2.9.9
asyncpg>=0.29.0

# ============================================
# Data Processing
# ============================================
numpy>=1.24.0
pandas>=2.0.0

# ============================================
# Utilities
# ============================================
python-dotenv>=1.0.0
python-multipart>=0.0.6

# ============================================
# Voice (STT/TTS) — 지연 로딩, 미설치 시 음성 엔드포인트만 503
# ============================================
faster-whisper>=1.0.0   # STT (온디바이스 Whisper)
edge-tts>=6.1.0          # TTS 기본 엔진(MS Edge, 한국어 최상). 'say'(macOS)는 무설치
# Mac GPU(Apple Silicon) STT 최속: pip install mlx-whisper
# 온디바이스 고품질 TTS(MeloTTS): pip install git+https://github.com/myshell-ai/MeloTTS.git
